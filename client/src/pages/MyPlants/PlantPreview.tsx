import PageHeader from "components/PageHeader";
import { FC } from "react";
import useLocalStorage from "shared/hooks/useLocalStorage";

const PlantPreview: FC = () => {
    return (
        <>
            <PageHeader title={"PlantPreview"} />
        </>
    );
};

export default PlantPreview;
