export enum Route {
    SIGN_IN = "/signin",
    MY_PLANTS = "/myPlants",
    REMINDERS = "/reminders",
    IDENTIFY = "/identify",
}

export enum MyPlantsRoute {
    PLANTS = "/plants",
    SITES = "/sites",
}

export enum PlantSettingsRoute {
    PREVIEW = "/preview",
    EDIT = "/edit",
    EDIT_REMINDERS = "/editReminders",
    SPECIES = "/species",
}
