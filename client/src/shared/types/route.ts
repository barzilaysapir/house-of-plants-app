export enum Route {
    HOME = "/",
    SIGN_IN = "/signin",
    MY_PLANTS = "/myPlants",
    REMINDERS = "/reminders",
    IDENTIFY = "/identify",
}

export enum MyPlantsRoute {
    PLANTS = "/",
    SITES = "/sites",
}
